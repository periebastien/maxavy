# Chargé automatiquement par `vite build` (mode production).
# Ce fichier peut être commité — il ne contient pas de secrets.
# Les clés Google et autres secrets restent dans .env (gitignored).
VITE_APP_URL=https://gmbmanager.ai
