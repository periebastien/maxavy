# CLAUDE.md — Locagain — SaaS E-Réputation / Gestion des avis Google

## Réponses
- Réponses courtes et concises uniquement
- Pas de résumé en fin de réponse
- Pas de commentaires évidents dans le code
- Propose, n'implémente pas sans validation explicite

## Projet
SaaS multi-tenant de gestion de la e-réputation et des avis Google My Business.
Référence visuelle : interface Qwairy (dashboard admin, sidebar gauche, accent violet #7C5CFC).
Cahier des charges complet : `CAHIER_CHARGES_MVP_EXTENSIBLE_FR.md`

## Stack technique
- **Frontend** : React 18 + Vite, JavaScript (pas TypeScript), TailwindCSS, React Router, Axios, Zustand
- **Backend** : Node.js 18+ + Express.js, JavaScript
- **ORM** : Sequelize (migrations incluses)
- **Auth** : JWT + bcryptjs
- **BDD** : PostgreSQL sur serveur OVH (`ns3181892.ip-146-59-148.eu`)
- **Déploiement** : OVH dédié via PM2 + Nginx reverse proxy (pas de Vercel)
- **Domaine** : locagain.com
- **Versioning** : GitHub — periebastien

## Structure du projet
```
backend/src/modules/[module]/   ← auth, businesses, reviews, widgets...
frontend/src/pages/[Page]/
frontend/src/components/common/ ← composants réutilisables
```

## Conventions
- Fichiers : kebab-case
- API REST — routes préfixées `/api/v1/`
- Variables `.env` — jamais committées, jamais en dur
- Isolation stricte par `business_id` à chaque requête backend
- Données personnelles chiffrées AES-256-GCM (email, téléphone, tokens OAuth)

## Commandes
```bash
# Backend (depuis backend/) :
node src/app.js          # démarrage dev
npm run dev              # nodemon (hot reload)

# Frontend (depuis frontend/) :
npm run dev              # port 5173
npm run build            # build prod
```

## Gestion des serveurs (2 process, 2 ports)
Deux serveurs distincts tournent en parallèle — ne jamais confondre :
- **Backend** — port **3000** (`node src/app.js` depuis `backend/`)
- **Frontend Vite** — port **5173** (`npm run dev` depuis `frontend/`)

**Redémarrer le backend moi-même** à chaque fois que nécessaire (nouvelles routes, modif `app.js`, nouveau module, changement dans un module backend comme `widget.runtime.js`, etc.) :
1. Tuer **uniquement** le process du port concerné : `Get-NetTCPConnection -LocalPort 3000 | Select-Object -ExpandProperty OwningProcess | Select-Object -First 1` → récupérer le PID, puis `Stop-Process -Id <PID> -Force` (remplacer `3000` par `5173` pour le frontend)
2. **Ne jamais faire `Get-Process node | Stop-Process`** — ça tue les DEUX serveurs (backend 3000 **et** Vite 5173)
3. Relancer : backend `cd backend && node src/app.js` en arrière-plan ; frontend (si mort par erreur) `cd frontend && npm run dev` en arrière-plan
4. Vérifier les logs backend : `PostgreSQL connecté` et `[cron] Job invitations planifiées démarré`

Note : le frontend Vite a du hot-reload (HMR) → il redémarre rarement ; le backend, lui, doit être relancé à la main après toute modif backend.

## État du projet (mise à jour 2026-07-02)
Sessions 1–26 terminées. Phase 7 complète.
**Phase 6 — synchro des avis basculée GMB → DataForSEO (session 21, 2026-07-02)** : l'API GMB (quota bloqué) est abandonnée pour la **lecture** des avis. Provider `business_data/google/reviews` (mêmes identifiants `.env` que le geogrid, `tasks_ready` séparé → pas de collision), table `review_sync_jobs`, cron boucle 60s (backfill depth 200 / incrémental depth 10), **gating par plan** (`module_quotas.reviews` : Starter quotidien 1440 / Pro 6h 360 / Agence 60 min ; Gratuit exclu, éditable Super Admin), échelonnement déterministe anti-rafale, index unique `reviews(platform,external_id)`. Migrations 43-45. ⚠️ Piège : `location_coordinate` **obligatoire** côté DataForSEO même avec `place_id` (sinon `40501 Invalid Field: location_name`). File standard en auto, priority pour le bouton manuel. Vérifié en réel (64 avis Atlas importés) + preview. Détail : `PROGRESS.md` Phase 6. **Répondre aux avis (session 22)** = 2ᵉ temps : génération IA prévue (prompt configurable), mais DataForSEO ne **publie pas** → canal d'écriture à trancher (API GMB ou saisie manuelle) ; module `google/` OAuth laissé **inerte**.
Phase 8 — sessions 26, 26b, 27 terminées (widgets backend + tags + carrousel/badge runtime + builder). Session 28 largement couverte en 27 (reste : polish badge, grep anti-fuite, « lire plus »).
Design system widgets (catalogue 5 widgets + modèle de config + gabarits) : **`WIDGETS_DESIGN_FR.md`**.
Suivi de positionnement (geogrid / heatmap Google Maps) — spec dans cahier §9.5 + **`GEOGRID_DESIGN_FR.md`**. Décisions : source = **DataForSEO** (identifiants `.env` en place et validés), facturation = **gating par plan** (Gratuit exclu, Starter bridé 5 mots-clés, Pro/Agence à ajuster). **Sessions G1, G2 et G3 terminées** (2026-07-01) : schéma + CRUD mots-clés (gating quota), provider DataForSEO (queue **Priority**) + `scan.service.js` (création/poll/finalisation) + endpoints scans, puis **cron** `jobs/scan-geogrid.js` (boucle `setInterval` 90 s : timeout + poll + lancement des mots-clés dus). Scalabilité (bcp de fiches) : lot `GEOGRID_BATCH_SIZE`/concurrence `GEOGRID_CONCURRENCY` bornés, pool PG relevé `DB_POOL_MAX`, détection « dû » via `geogrid_keywords.last_scanned_at`, timeout `GEOGRID_SCAN_TIMEOUT_MINUTES` — tous réglables dans `.env`. **Testé cron réel de bout en bout** (mot-clé → scan lancé et terminé en autonomie, 122 s). **Session G4 terminée** (2026-07-01) : `GeogridPage` (`/positionnement`, lazy) + `GeogridMap` (carte Google Maps, marqueur `Marker` legacy = pastilles de rang colorées + InfoWindow concurrents) + gestion mots-clés + gating par plan + scan avec **polling** (nouveau pattern front) + métriques ARP/ATRP/SoLV. Vérifié en preview avec données réelles (structure/metrics/API OK) ; la carte Maps ne se peint pas en preview **headless** (limitation navigateur automatisé, pas un bug — API Maps brute idem) mais rend en vrai navigateur. ⚠️ **Démo laissée** : Atlasimmobilier passée en plan Starter + 1 scan pour vérif visuelle utilisateur (reverter `plan_id`→null quand fini). Détail : `PROGRESS.md` Phase 11. **Refonte « Positionnement » cadrée (2026-07-02)** : cahier des charges dédié **`GEOGRID_REFONTE_FR.md`** (assistant de config en étapes : grille déplaçable + forme carré/cercle ; page Suivi lecture seule + courbes Recharts ; **concurrents** ; **rapport email** ; planning fin mensuel/hebdo/quotidien + fuseau par localisation, défaut = fuseau entreprise, éditable ; quotas plan **éditables Super Admin**, par localisation). Nouvelles sessions **G5→G12** (voir `PLAN_SESSIONS.md` Phase 11). ⚠️ L'UX G1→G4 (`GeogridPage`) est un socle, pas la cible finale. **Session G5 terminée (2026-07-02)** : migration **additive uniquement** (4 nouvelles tables `geogrid_configs`/`geogrid_competitors`/`geogrid_runs`/`geogrid_scan_competitors` + colonnes `config_id`/`run_id`/`points_top3-10-20` + migration des données existantes + quotas plan enrichis sans retirer les anciennes clés) — **zéro régression**, cutover reporté à G6. **Session G6 terminée (2026-07-02)** : cutover complet — grille cercle (masque disque, `buildGrid(...,shape)`), planning fuseau-aware (`schedule.utils.js`, lib **Luxon** ajoutée, `computeNextRunAt` testé 7 cas), cron réécrit par runs/configs (`findDueConfigs`/`launchRunForConfig`/`closeFinishedRuns` remplacent l'ancienne détection par mot-clé), auto-provisioning de config (`ensureConfigForLocation`), retrait définitif de `grid_size`/`grid_spacing_m`/`frequency`/`last_scanned_at` du mot-clé (migrations 40-41, `POST /scans` manuel inchangé). Vérifié en conditions réelles : cycle complet run→scan sur un vrai appel DataForSEO (grille 3×3 temporaire, coût minimal), 4 scénarios `closeFinishedRuns` en isolation, non-régression `/positionnement` confirmée en preview. **Sidebar « POSITIONNEMENT » anticipée de G8** (nav seule, 2026-07-02) : Configuration/Suivi/Concurrents, route renommée `/positionnement` → `/positionnement/configuration`. **Session G7 terminée (2026-07-02)** : CRUD concurrents (`competitor.service.js`, quota par config = par localisation), agrégats fiche+concurrents (top 3/10/20, `NOT_RANKED=21` imputé hors profondeur mesurée), `MAX_COMPETITORS` 5→20 (coût nul, profondeur déjà là), backfill des scans historiques, endpoints `GET/PUT /config` (édition stricte, plafonds du plan), `GET/POST/DELETE /competitors` + `/recompute`, `POST/GET /runs` (rapport manuel multi-mots-clés, `POST /scans` mono-mot-clé conservé pour l'UI actuelle), `GET /trend`. Quota mots-clés basculé par localisation (décision actée, différée depuis G5/G6). Vérifié en conditions réelles (dont un vrai `POST /runs`), données de test nettoyées, config restaurée à l'identique. **G8 découpé en 3 sous-sessions.** **G8.1 terminée (2026-07-02)** : 1ʳᵉ UI visible de la refonte — `StepIndicator` (réutilisable) + `GeogridConfigMap` (carte à marqueur central déplaçable) + `GeogridConfigPage` (wizard 4 étapes, **Étape 1 Grille complète** : carré/cercle, densité/espacement, compteur live points·couverture·coût, `PUT /config`). Routage : wizard sur « Configuration », ancienne `GeogridPage` déplacée sur « Suivi » (dégrisée) pour garder l'accès aux résultats jusqu'à G9. Vérifié en preview (49 pts carré → 29 cercle, save OK, config intacte). **Révisée sur retours utilisateur** (2026-07-02) : carte pleine largeur en haut / config dessous / stats à côté du bouton, points plus gros et foncés, coût en $ retiré, et surtout **cercle = disque des N² points les plus proches du centre** (49 points formant un vrai cercle, pas un carré — 2 itérations avant la bonne version). `GEOGRID_REFONTE_FR.md` mis à jour en conséquence. **Session G8.2 terminée (2026-07-02)** : Étape 2 (Mots-clés, reprise du pattern d'ajout/suppression existant) + Étape 3 (Planning : fréquence bridée par plan, heure/jour selon fréquence — convention `run_day_of_week` 0=dimanche..6=samedi alignée avec `schedule.utils.js` —, fuseau via la liste `TIMEZONES` déjà utilisée dans Onboarding/Settings), sauvegarde `PUT /config`, affichage du `next_run_at` recalculé. 100% frontend, aucun backend touché. Vérifié différemment cette fois : l'utilisateur a repris la main sur le serveur frontend (terminal manuel) donc pas de pilotage navigateur — vérifié via `esbuild` (syntaxe) + script Node authentifié contre l'API réelle (PUT /config mensuel → next_run_at juste, restauration exacte, cycle mot-clé propre). **Session G8.3 terminée (2026-07-02) — G8 complet.** Petit ajout backend annoncé : `GET /competitors/detected` (concurrents vus dans les 20 derniers scans, non suivis, triés par meilleur rang — testé en direct : 30 résultats réels). Étape 4 (Concurrents, optionnelle, skippable) : `PlaceSearch` réutilisé (remonté via `key` pour se réinitialiser entre deux ajouts, sans toucher au composant partagé) + puces de concurrents détectés. Étape 5 (Récap, pas un 5ᵉ point du stepper) : résumé des 4 dimensions avec liens « Modifier », bouton « Lancer un premier rapport maintenant » (`POST /runs`, chemin déjà prouvé en G7). Mode édition minimal : une fiche déjà configurée (≥1 mot-clé) atterrit direct sur le récap avec toutes les étapes déverrouillées (réutilise la logique existante du `StepIndicator`, aucun code dédié). L'ancienne `GeogridPage` reste sciemment à `/positionnement/suivi` — son retrait est repoussé à G9 (qui la remplace vraiment), pas cette session. Vérifié via `esbuild` + tests API réels (detected, cycle CRUD concurrents) ; entre-temps l'utilisateur a lui-même ajouté 4 mots-clés réels via l'Étape 2, confirmant G8.2 en conditions réelles. **2 correctifs G8.3 le même jour** (retours utilisateur) : (1) tri des concurrents détectés par visibilité réelle (`avg_position` façon ATRP + `avg_rank_when_seen` affiché, plus par simple meilleur rang) ; (2) Étape 4 consciente du contexte (`hasCompletedRun` via `GET /runs`) — affiche un bouton « lancer un premier rapport » au lieu d'une détection silencieusement vide sur une fiche neuve. Détail complet : `PROGRESS.md` Phase 11. Rough edge repéré non corrigé : `step` du wizard ne se réinitialise pas à 1 en changeant de localisation en cours de route. **G9 découpé en 3 sous-sessions (G9.1/G9.2/G9.3).** **G9.1 terminée (2026-07-02)** : nouvelle page `GeogridSuiviPage.jsx` (route de dev temporaire `/positionnement/suivi-v2`, pas encore liée dans la sidebar) — sélecteur de rapport + tableau par mot-clé (ATRP, top 3/10/20) + flèche d'évolution vs rapport précédent (calcul front, ATRP=rang donc baisse=amélioration). Aucun backend nécessaire (tout existait depuis G7). Bug « Invalid Date » corrigé (`createdAt` camelCase géré par Sequelize, pas `created_at`). Vérifié en preview avec les vraies données (5 mots-clés du rapport manuel déjà lancé par l'utilisateur). **Session G9.2 terminée (2026-07-02)** : `npm install recharts` (3.9.1) + nouvelle brique `lib/geogrid-trend.js` (bucketing jour/semaine/mois, agrégation moyenne/meilleure position, filtre de plage 30j-tout, fusion multi-séries) — testée en isolation (6 cas) avant intégration, piège UTC de `toISOString()` évité dès l'écriture (clés de bucket en composants de date locaux uniquement). Courbe multi-mots-clés câblée sur la vue globale, axe Y inversé (rang 1 en haut). Vérifié en preview (SVG + 5 lignes + sélecteurs fonctionnels), capture d'écran impossible (limitation headless connue depuis G4, pas un bug). Incident de session sans rapport avec le code : rechargements répétés → rate-limit API épuisé → déconnexion, résolu par redémarrage backend + reconnexion. **Session G9.3 terminée (2026-07-02) — G9 complet.** Backend : `getScan()` renvoie aussi les concurrents du scan (`GeogridScanCompetitor` triés par `avg_position`), champ additif rétro-compatible. Frontend : clic sur une ligne du tableau (Vue Globale) → Vue Par Mot-Clé (4 `MetricCard` ARP/ATRP/SoLV/note, `GeogridMap` heatmap réutilisée, `CompetitorTable` triable ma-fiche+concurrents, courbe mono-série réutilisée de G9.2, bouton Retour). Fix au passage : l'ajout d'un concurrent (wizard G8.3) ne recalculait pas les scans historiques déjà passés — `recompute` désormais déclenché automatiquement après chaque ajout. **Bascule finale de route** : `/positionnement/suivi` pointe directement sur `GeogridSuiviPage`, ancienne `GeogridPage.jsx` supprimée (plus aucune référence, `GeogridMap.jsx` conservé). Vérifié en preview sur la vraie route (Vue Globale → clic → Vue Par Mot-Clé avec vraies données → Retour). **Session G10 terminée (2026-07-03)** : nouvel endpoint `GET /competitors/trend` (série `avg_position` par concurrent, alignée sur les mêmes scans que `GET /trend` — seule pièce backend manquante, le reste existait depuis G7/G9.3). `CompetitorTable`/`TrendControls`/`TrendChart` extraits de `GeogridSuiviPage.jsx` vers des composants partagés (`components/GeogridCompetitorTable.jsx`, `components/GeogridTrendChart.jsx`) pour être réutilisés sans duplication. Nouvelle page `GeogridConcurrentsPage.jsx` (`/positionnement/concurrents`, sidebar activée) : sélecteur mot-clé/rapport + tableau + courbe multi-séries (ma fiche vs chaque concurrent) + état vide dédié si aucun concurrent suivi + lien « Gérer les concurrents ». Petit ajout : `GeogridConfigPage.jsx` lit `?step=N` pour sauter directement à une étape du wizard (sert ce lien, atterrit sur l'étape 4). Bug pré-existant corrigé : `.claude/launch.json` manquait `cwd: "backend"` pour la config `locagain-backend`. Vérifié en preview avec données réelles + non-régression complète de `GeogridSuiviPage` après l'extraction des composants partagés. Détail complet : `PROGRESS.md` Phase 11. **Session G10.5 — résilience du cron geogrid (2026-07-03)** : correctif hors-plan déclenché par un rapport hebdo perdu sur un blip réseau (`fetch failed` transport → une semaine perdue car `next_run_at` avancé avant le scan). **Découplage cadence/reprise** : la cadence (`geogrid_configs.next_run_at`) n'est plus jamais touchée par une reprise ; l'état de reprise vit dans des colonnes séparées, tout en DB (survit au redémarrage). Migrations 50-52 additives (`attempts`/`next_attempt_at`/`retry_reason` sur scans, `attempts`/`next_attempt_at`/`notify_failure` sur runs, statut `retry_pending`). **Retry 3 niveaux** : A = scan transport (0 tâche postée) → re-submit **en place** (jamais de scan dupliqué), backoff **espacé** 10/30/90 min + **jitter déterministe** anti-rafale, max 3 ; B = partiel/timeout (tâches déjà payées) → re-poll **direct** `task_get`, coût 0 $, fenêtre 6 h ; C = run inexploitable → replanification (30/120 min, max 2). **Anti-double-facturation stricte** : `postScanTasks` ne re-POSTe que les points sans task, jamais `task_post` à l'aveugle (pas de clé d'idempotence DataForSEO) + adoption best-effort des tâches déjà créées (crash-pendant-POST). **Étalement multi-tenant** : plafond points en vol (`GEOGRID_MAX_POINTS_IN_FLIGHT`, protège la file 1000) + **circuit-breaker** (N échecs transport consécutifs → pause lancements ET reprises). Provider : distinction transport (`err.transient`, retenté) vs erreur métier (définitive), retry court sur GET idempotents seulement. `computeNextRunAtSkipping` (saut des périodes ratées → pas de rafale de rattrapage après coupure). `notify_failure` = **hook** posé pour l'alerte email G11. 9 paramètres `.env` (`GEOGRID_MAX_SCAN_ATTEMPTS`, `_BACKOFF_SCAN`, `_BREAKER_THRESHOLD`…). Vérifié contre la vraie base (jamais de mock) : unitaires (19), test contrôlé Level A 12/12 (reprise sans doublon ni double facturation, run non clôturé prématurément), test Level C/circuit/annulation 10/10. Détail : `GEOGRID_REFONTE_FR.md §7.1` + `PROGRESS.md` Phase 11. **Session G10.6 — refonte UX Suivi + Concurrents (2026-07-03)** : page Suivi **unifiée** (fin de la scission vue globale/vue par mot-clé). Sélecteur de mot-clé (dont « **Moyenne globale** ») pilotant **grand graphe** + **grande carte** pleine largeur ; **clic sur un jour** de la courbe → met à jour la carte (`onDayClick`→`selectedRunId`) ; carte moyenne globale = heatmap de rang moyen par point (nouvel endpoint `GET /runs/:id/average-map`). Mode mot-clé : métriques ARP/ATRP/SoLV/note + tableau concurrents ; tableau d'évolution (clic ligne → focus mot-clé). Même gabarit sur **Concurrents** (grand graphe de comparaison + grande carte = ma heatmap + tableau). Composants : `GeogridMap` prop `heightClass`, `TrendChart` props `onDayClick`/`height`, `bucketKeyOf` exporté. Vérifié en preview réel (2 pages, données réelles, 0 erreur) ; clic-jour câblé + moitié aval vérifiée (simulation d'événements internes Recharts impossible en headless, comme Maps). Détail : `GEOGRID_REFONTE_FR.md §8/§9` + `PROGRESS.md` Phase 11. **Session G10.7 — portail débit/concurrence DataForSEO (2026-07-04)** : suite à « des requêtes qui n'aboutissent pas » (timeouts par rafales). Audit : compte DataForSEO **partagé** entre crons geogrid+avis, limites 2000/min total + **60/min sur tasks_ready** + file 1000. Nouveau **portail global** `services/dataforseo-gate.js` (`schedule(fn, kind)` : sémaphore de concurrence + débit/min à fenêtre glissante + sous-plafond tasks_ready, FIFO), branché sur les **deux** providers → charge combinée bornée. AbortSignal créé dans le thunk (le timeout ne compte pas l'attente en file). Fix : un seul `tasks_ready` partagé par lot de reprises. Réglages `DATAFORSEO_MAX_CONCURRENCY`/`_MAX_PER_MINUTE`/`_TASKS_READY_PER_MINUTE`. Vérifié (unitaire sémaphore/sous-plafond + appels réels via les 2 providers). ⚠️ Le câblage du provider **avis** touche un fichier de la session parallèle (autorisé). Détail : `PROGRESS.md` Phase 11. Prochaine session : **G11 — Rapport email (v1)** (dont consommation du hook `notify_failure`).
**Suivi des avis de la concurrence cadré (2026-07-03)** : spec dédiée **`AVIS_CONCURRENTS_FR.md`**, sessions **AC1→AC3** (`PLAN_SESSIONS.md`/`PROGRESS.md` Phase 12). Décisions : source DataForSEO (Places n'a pas d'historique) ; liste de concurrents **partagée** avec le geogrid *pour l'instant* (données découplées par `place_id`, aucune FK → séparation future = 1 fonction) ; quota = `rank_tracking.max_competitors` existant (3/5/10) ; synchro quotidienne échelonnée via le cron sync-reviews ; courbes = `TrendChart` geogrid étendu par props (axe Y non inversé). **Session AC1 terminée (2026-07-03)** : migrations 47-49 (`competitor_reviews`/`review_competitor_tracking`/`review_sync_jobs.competitor_place_id`), réconciliation depuis `geogrid_competitors` (idempotente, auto-réparatrice), `resolveJob` scindé ma-fiche/concurrent (poll `tasks_ready` unifié). Vérifié en réel : 175 vrais avis concurrents importés (3 concurrents Atlasimmobilier), place_id concurrent + coordonnées de la localisation validés côté DataForSEO, 0 interférence avec la synchro "ma fiche". **Session AC2 terminée (2026-07-03)** : migration 50 (`locations.total_reviews_count`/`avg_rating`, symétrique côté "ma fiche" — requis par la règle de complétude qui s'y applique aussi) ; endpoint `GET /reviews/competitors/stats` (`getCompetitorStats`, développé via workflow explore→implement→verify). Fuseau horaire **vérifié empiriquement** contre la vraie connexion Postgres (session UTC confirmée, `date_trunc` correct tel quel — un correctif `AT TIME ZONE 'UTC'` envisagé aurait en réalité introduit un décalage d'un jour, testé et rejeté) : le finding "high" de la revue adversariale reposait sur une hypothèse fausse, invalidé en réel plutôt qu'appliqué aveuglément. Finding réel corrigé : `available_years` inclut désormais toujours l'année demandée. Vérifié contre les vraies données AC1 (agrégats mensuels, complétude, fallback année invalide, localisation sans concurrent). ⚠️ Session concurrente sur ce même projet (retry geogrid, migrations 50-52 `geogrid_scans`/`geogrid_runs` — collision de numérotation bénigne avec notre migration 50, tables différentes). **Session AC3 terminée (2026-07-03) — module « Suivi des avis de la concurrence » COMPLET (AC1→AC3).** `ReviewsConcurrentsPage.jsx` (`/reviews/concurrents`, sidebar AVIS) : gestion des concurrents (quota partagé 3/5/10, PlaceSearch, détectés, backfill priority ~1 min à l'ajout via nouvelle route `POST /reviews/competitors/sync`), carte "Ce mois-ci", courbe 12 mois, tableau mensuel, sélecteur d'année. `TrendChart` étendu par props (`yReversed`/`yLabel`, défauts inchangés). 4 findings adversariaux confirmés et corrigés, dont un réel (pas théorique) : collision de clé de courbe pour concurrents homonymes — "Guy Hoquet" (franchise) est un des 3 vrais concurrents suivis ; corrigé en séparant `dataKey` (place_id, unique) du libellé affiché. ⚠️ Piège rencontré en vérif : le serveur backend en cours servait du code désynchronisé (nodemon pas rechargé) → `last_synced_at` affichait "jamais" en preview alors que la base avait la vraie date ; redémarrage backend a résolu — ne jamais faire confiance à l'écran sans vérifier la donnée réelle en base en cas d'incohérence. Vérifié en réel (données Atlasimmobilier, quota bloquant, courbe 4 lignes sans collision) + non-régression positionnement confirmée. **Refonte UX AC3 sur retours (2026-07-03)** : carte « Ce mois-ci » → grille de `MetricCard` (Ma fiche + tous les concurrents) ; ordre cartes→courbe→tableau→gestion ; pleine largeur + tableau `table-fixed` (colonnes proportionnelles) ; **charte de rang extraite dans `frontend/src/lib/rank-palette.js`** (source unique réutilisable, `lib/geogrid.js` ré-exporte → zéro régression carte) + cellules du tableau colorées par **classement relatif du mois** (leader vert / dernier rouge / entre-deux orange). Détail complet : `PROGRESS.md` Phase 12.
Responsive (2026-07-01) : cahier des charges §8.7 ajouté (full responsive, mobile-first) + **admin rendu full responsive** (sidebar en drawer + burger, grilles adaptatives) — session 33 partielle (reste états vides/loaders). Compte de test preview en mémoire.
Widgets — polish badge (2026-07-01) : option `containerPadding` (marge intérieure), défaut `align` passé à `center`, « Propulsé par Locagain » toujours centré sous le badge (jamais flush sur son bord gauche/droit) — voir `WIDGETS_DESIGN_FR.md` §3.1.
**Correctif transverse isolation par localisation (2026-07-04)** : toutes les pages sidebar suivent désormais `activeLocation` (ReviewsPage recâblée — son dropdown local supprimé, migration 53 `customers.location_id` NULL=toutes fiches, invitations persistent enfin `location_id`, campagnes/widgets/stats filtrés, Dashboard par fiche, faille widgets cross-tenant fermée via `assertOwnership`). Restent à traiter (audit) : `POST /credits/add` sans contrôle de rôle, index unique global `reviews(platform,external_id)` = vol d'avis possible si 2 fiches partagent un place_id, `business.middleware.js` code mort. Détail : `PROGRESS.md` « Correctif transverse ».
Prochaine session : **28 (finitions widgets)** ou **29 — Paramètres entreprise** (module avis concurrents AC1→AC3 terminé). Bug corrigé (2026-07-01) : modèle `Review` avait `updated_at` absent de la table → cassait toute lecture d'avis, fix `updatedAt: false`. Config Stripe (clés + stripe_price_id des plans) à faire séparément.
Voir `PROGRESS.md` pour le détail complet.

## Fin de session — checklist obligatoire
À chaque fin de session (avant le commit) :
1. Mettre à jour `PROGRESS.md` — ligne de la session passée `⬜` → `✅` avec notes courtes
2. Mettre à jour `CLAUDE.md` — bloc « État du projet » (sessions terminées, prochaine session)

## Règles de développement
- Chaque module = dossier indépendant (`routes.js` + `controller.js` + `service.js`)
- Ajouter un module = nouveau dossier + entrée `business_modules`, jamais de refonte
- Tests : contre une vraie base PostgreSQL, jamais de mock
- Super Admin : periebastien@gmail.com
